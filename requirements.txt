# Core
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.1.0

# Embedding & ML
sentence-transformers>=2.5.0
torch>=2.0.0

# Vector search
faiss-cpu>=1.7.4

# Keyword search
rank-bm25>=0.2.2

# HTTP client
httpx>=0.27.0

# Utilities
numpy>=1.24.0
PyMuPDF>=1.24.0

# Optional: Reranker (uncomment if needed)
# cross-encoder models are included in sentence-transformers

# Frontend (optional)
streamlit>=1.30.0

# OPTIMIZED_BY_CODEX_STEP_2
apscheduler>=3.10.4
datasets>=2.19.0
plotly>=5.22.0
pytest>=8.2.0
pytest-asyncio>=0.23.8
ragas[all]>=0.1.10
# STEP_2_SUMMARY: Added evaluation dependencies for RAGAS pipeline, dataset handling, and chart rendering.
# STEP_3_SUMMARY: Added APScheduler dependency for weekly automated index refresh jobs.
# STEP_4_SUMMARY: Added pytest dependencies for local and CI test execution.
